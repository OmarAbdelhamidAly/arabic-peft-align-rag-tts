# ============================================================
# Service Training - Requirements
# GPU Setup: RTX 3070 Ti | CUDA 12.6 | Python 3.10
# Conda Env: unsloth_env
#
# ⚠️  Installation order (IMPORTANT):
#   1. conda create -n unsloth_env python=3.10 -y
#   2. conda activate unsloth_env
#   3. Install PyTorch via CONDA (NOT pip) to avoid DLL errors on Windows:
#      conda install pytorch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 pytorch-cuda=12.1 -c pytorch -c nvidia -y
#   4. pip install -r requirements.txt
# ============================================================

# --- PyTorch: يتثبت عن طريق conda (انظر الخطوة 3 أعلاه) ---
# conda install pytorch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 pytorch-cuda=12.1 -c pytorch -c nvidia -y

# --- Attention Acceleration ---
# ⚠️ لازم --no-deps عشان ميعملش downgrade للـ torch
# pip install xformers==0.0.27.post2 --index-url https://download.pytorch.org/whl/cu121 --no-deps
xformers==0.0.27.post2

# --- Unsloth (Fast Fine-Tuning Framework) ---
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# --- Training & Alignment ---
trl>=0.8.6
peft>=0.10.0
accelerate>=0.27.0
bitsandbytes>=0.42.0

# --- Data & Datasets ---
datasets>=2.18.0
pandas>=2.0.0

# --- Utilities ---
python-dotenv>=1.0.0
matplotlib>=3.8.0
openai>=1.0.0
